{
  "name": "@log-listener-plugin/monorepo",
  "scripts": {
    "log:build": "yarn workspace @wutiange/log-listener-plugin build",
    "log:dev": "yarn workspace @wutiange/log-listener-plugin dev",
    "log:clean": "yarn workspace @wutiange/log-listener-plugin clean",
    "example:android": "yarn workspace example android",
    "example:ios": "yarn workspace example ios",
    "example:start": "yarn workspace example start",
    "test": "jest"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "private": true,
  "packageManager": "yarn@4.5.3",
  "devDependencies": {
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
